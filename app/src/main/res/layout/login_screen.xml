<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/circular_blue">
    <ImageView
        android:id="@+id/banner_top"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/placeholder"
        android:src="@drawable/banner"
        android:paddingBottom="8dp"
        />
    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginTop="16dp"
        android:layout_marginStart="20dp"
        android:contentDescription="@string/placeholder"
        android:src="@drawable/ila_icon"
        android:layout_alignStart="@id/banner_top"
        android:layout_alignTop="@id/banner_top"/>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/banner_top">

        <androidx.cardview.widget.CardView
            android:layout_height="200dp"
            android:layout_width="350dp"
            android:padding="16dp"/>
        <EditText
            android:layout_width="350dp"
            android:layout_height="wrap_content"
            android:autofillHints="@string/placeholder"
            android:inputType="textEmailAddress"
            android:text="@string/placeholder"
            android:gravity="center_horizontal"
            />

        </LinearLayout>

</RelativeLayout>